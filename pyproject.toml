[project]
name = "omniparser"
version = "0.0.1"
readme = "README.md"

[tool.setuptools.packages.find]
where = ["."]
include = ["omniparser"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
